First commit
- use heroku plugins:install ssh://git@github.com/ddollar/heroku-sql-console.git for easy heroku sql access
- use 'export DATABASE_URL=postgres://localhost/keegan' for using a local postgres db
